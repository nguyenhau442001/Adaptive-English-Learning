{
  "name": "adaptive-english-learning",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/web",
    "packages/*",
    "packages/exam-profiles/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "seed:toeic": "npm run seed --workspace=@aelearning/exam-profile-toeic"
  }
}
